# Evolutionary_synthesizer_for_logic_gates
A c++ script that performs an evolutionary approach to convert truth tables to logic gates efficiently while trying to minimize the gates count as much as possible
